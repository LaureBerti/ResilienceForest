# E1 data prep — IFL conversion + climate download
# Requires Python 3.12 (same venv as full pipeline, no separate env needed)
# Does NOT require stumpy / numba / LLVM.
pandas>=2.2.3
numpy>=1.26.4
pyarrow>=18.0.0
geopandas>=1.0.0
hydra-core>=1.3.2
omegaconf>=2.3.0
requests>=2.31.0
gdown>=5.2.0
